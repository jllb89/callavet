# Copy to the VM and source it before running docker compose.
# export $(cat env/.env.staging.example | xargs)  # quick and dirty; prefer proper secret management in production.

# Compose variables
REGISTRY_OWNER=your-github-org-or-user
TAG=staging

# Gateway
NODE_ENV=staging
DATABASE_URL=postgres://USER:PASS@host:5432/db_name?sslmode=require
SUPABASE_JWT_SECRET=your_supabase_jwt_secret
STRIPE_SECRET_KEY=sk_test_...

# Twilio (for Global Safe List operations during OTP testing)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_SAFE_LIST_TEST_NUMBER=+525542850675

# Webhooks
STRIPE_WEBHOOK_SECRET=whsec_...

# Smoke tests and docs
# API_BASE is used by env/scripts/smoke-staging.sh (defaults to http://localhost:4000)
# API_BASE=https://staging.call-a-vet.app
